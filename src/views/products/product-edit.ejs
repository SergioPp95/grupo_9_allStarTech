<!DOCTYPE html>
<html lang="en">
<!-- Head -->
<%- include("../partials/head") %>

    <body>
        <!-- Header -->
        <%- include("../partials/header") %>
            <main>
                <form class="form-crud" action="/products/<%= product.id %>?_method=PUT" method="POST" enctype="multipart/form-data">
                    <div class="crud-info">
                        <div>
                            <label for="name">Titulo</label>
                            <input class="searchInput" type="text" name="name" value= "<%= product.name %>"  >
                        </div>
                        <div>
                            <label for="price">Precio</label>
                            <input class="searchInput" type="text" name="price" value= "<%= product.price %>"  >
                        </div>
                        <div>
                            <label for="discount">Descuentos</label>
                            <input class="searchInput" type="text" name="discount" value= "<%= product.discount %> "  >
                        </div>
                        <div>
                            <label for="description">Descripcion del producto:</label>
                            <textarea class="searchInput" type="textarea" name="description"> <%= product.description %> </textarea>
                        </div>
                        <div>
                            <label for="image1">Seleccionar imagen principal:</label>
                            <input class="searchInput" type="file" name="image1">
                        </div>
                        <div>
                            <label for="image2">Seleccionar imagen Secundaria:</label>
                            <input class="searchInput" type="file" name="image2">
                        </div>
                    </div>
                    <div>
                        <label for="category">Categoria Del Producto:</label>
                        <select name="category">
                            <option value="computer">Computadora</option>
                            <option value="screen">Pantalla</option>
                            <option value="phone">Celular</option>
                            <option value="audio">Audio</option>
                            <option value="other">Otro</option>
                        </select>
                    </div>
                    <div>
                        <button type="submit"> Aplicar cambios</button>
                    </div>
                </form>
                <form action="/products/<%= product.id %>?_method=DELETE"  method="POST">
                    <button type="submit">Eliminar producto</button>
                </form>

            </main>
            <!-- Footer -->
            <%- include("../partials/footer") %>
    </body>

</html>